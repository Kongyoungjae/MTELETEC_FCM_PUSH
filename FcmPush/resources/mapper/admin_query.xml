<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="admin">
    
    <select id="selectPushInfo" resultType="hashmap" >
        SELECT *
          FROM TB_PUSH
        WHERE PUSH_ID = 1
        LIMIT 1
    </select>
    
    <select id="selectUsersToken" resultType="String" >
        SELECT 
        	FCM_TOKEN
          FROM TB_USER
        ORDER BY REG_DT DESC
    </select>

    <select id="selectSMS" parameterType="hashmap" resultType="hashmap">
        select
			SENDER
			,RECVER
			,MSG
        from
			SMS_SEND_HIST
		where
			SEQ = #{seq}
    </select>

    <update id="updateSMS" parameterType="hashmap">
    	update
			SMS_SEND_HIST
    	set
			SEND_DT = SYSDATE()
		where
			SEQ = #{seq}
    </update>

</mapper>